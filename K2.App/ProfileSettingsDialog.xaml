<Window x:Class="K2.App.ProfileSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:K2.Core;assembly=K2.Core"
        Title="{loc:Get profile_settings_title}"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Style="{DynamicResource K2WindowStyle}">

    <StackPanel Margin="16">
        <TextBlock Text="{loc:Get profile_settings_title}"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

        <TextBlock Text="{loc:Get profile_settings_name_label}" Foreground="#AAA" FontSize="11" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtName" Margin="0,0,0,12" Height="25"/>

        <TextBlock Text="{loc:Get profile_settings_exe_label}" Foreground="#AAA" FontSize="11" Margin="0,0,0,4"/>
        <Grid Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtExePath" Grid.Column="0" IsReadOnly="True" Margin="0,0,6,0" Height="25"/>
            <Button Grid.Column="1" Content="{loc:Get browse}" Click="BtnBrowse_Click" Margin="0,0,4,0" UseLayoutRounding="True" ScrollViewer.CanContentScroll="True" Padding="12,0,12,0"/>
            <Button Grid.Column="2" Content="{loc:Get profile_settings_clear}" Click="BtnClearExe_Click" UseLayoutRounding="True" ScrollViewer.CanContentScroll="True" Padding="12,0,12,0"/>
        </Grid>
        <TextBlock Text="{loc:Get profile_settings_exe_hint}" Foreground="#777" FontSize="10"
                   TextWrapping="Wrap" Margin="0,0,0,12"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="{loc:Get delete_profile}" Click="BtnDelete_Click" Padding="12,0,12,0"/>
            <Button Grid.Column="2" Content="{loc:Get ok}"
                    IsDefault="True" MinWidth="72"
                    Style="{StaticResource K2IconAccentButton}"
                    Click="BtnSave_Click" Margin="0,0,8,0"  Grid.IsSharedSizeScope="True" Padding="0,0,7,0"/>
            <Button Grid.Column="3" Content="{loc:Get cancel}" IsCancel="True" MinWidth="72" Padding="12,0,12,0"/>
        </Grid>
    </StackPanel>
</Window>
